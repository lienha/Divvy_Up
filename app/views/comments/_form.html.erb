<%= form_for(:comment, url: poi_comments_path(poi.id)) do |f| %>
  <p>
    <%= f.hidden_field :poi_id, {value: poi.id} %>
    <%= f.label :text, "Comment on this resource:" %><br>
    <%= f.text_area :text %>
  </p>
  <p>
    <%= f.submit "Comment"%>
  </p>
<% end %>
