<% if current_user %>
  <%= form_for(poi.comments.new) do |f| %>
    <p><%= f.label :text, "Leave comment:" %><br>
      <%= f.text_area :text %>
    </p>
    <p><%= f.submit "Comment"%></p>
  <% end %>
<% else %>
  <%= link_to 'signin-able', signin %> or <%= link_to 'signup', signup %>
